{% extends 'base.html.twig' %}

{% block title %}Add Product | Therightcorner{% endblock %}

{% block body %}

    {{ include('includes/navbar.html.twig') }}}

    

<div class="d-flex justify-content-center">
    <div class="rounded bg-orange-lighter w-50 shadow p-4 my-3">
        <h1 class="text-center mb-5 bg-light p-2 rounded shadow" style="position: absolute; margin-top:-2em">Add a product</h1>
        {{ include('product/_form.html.twig') }}
    </div>
</div>


    <a href="{{ path('dashboard') }}" class="btn btn-sm btn-secondary">Dashboard</a>
{% endblock %}
